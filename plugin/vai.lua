-- vai.nvim plugin loader
-- This file is automatically sourced by Neovim

-- Don't load if already loaded or in compatible mode
if vim.g.loaded_vai or vim.g.compatible then
	return
end

vim.g.loaded_vai = true
